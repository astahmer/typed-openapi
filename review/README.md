# PR review — `snwootwz` → tip

Professional review of jj revisions from `snwootwz` (`test: stop filtering TS2456…`) through `kpovwyyt` / tip (`style: oxfmt…`).

## Scope

| Oldest | Newest (at review start) |
|--------|--------------------------|
| `snwootwz` / `4127caec` | `kpovwyyt` / `e7b64bb4` |

**Out of range (context only):** `mtnswwoy` (JSON Schema `const` → literal) and `wmnmwstk` (recursive interfaces). Those enable the TS2456 unfilter; not re-reviewed here.

## How to read

- [`comments/`](./comments/) — one file per finding (`C001`…)
- Status: `open` | `resolved`
- [`conclusion.md`](./conclusion.md) — only **open** items + follow-ups

When a later revision fixes an earlier comment, that comment’s status becomes `resolved` with a **Resolved in** pointer.
