save-exact=true
prefer-offline=true
strict-peer-dependencies=false
resolve-peers-from-workspace-root=true
enable-pre-post-scripts=true
# Peers for typed-openapi (zod/effect/…) stay optional — install only via explicit
# package.json entries (devDependencies in this monorepo / app deps for consumers).
auto-install-peers=false
# Dual-major effect: keep effect@3 nested under @effect/schema (see .pnpmfile.cjs).
dedupe-peer-dependents=false
